{
  "name": "trellike",
  "version": "0.0.1",
  "description": "Starting point for a kanban board UI",
  "keywords": [],
  "main": "src/index.js",
  "dependencies": {
    "react": "18",
    "react-dom": "18"
  },
  "devDependencies": {
  },
  "scripts": {
    "dev": "esbuild --bundle ./src/index.js --outdir=public --loader:.js=jsx --servedir=public",
    "build": "esbuild --bundle ./src/index.js --outdir=public --loader:.js=jsx --minify"
  }
}
